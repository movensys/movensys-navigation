name: Lint

on:
  pull_request:
    branches: [main, devel]

defaults:
  run:
    shell: bash

jobs:
  lint:
    name: Lint (${{ matrix.ros_distro }})
    runs-on: ${{ matrix.os }}
    container: ros:${{ matrix.ros_distro }}-ros-base
    strategy:
      fail-fast: false
      matrix:
        include:
          - ros_distro: humble
            os: ubuntu-22.04
          - ros_distro: jazzy
            os: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - name: Install lint tooling
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            ros-${{ matrix.ros_distro }}-ament-cpplint \
            ros-${{ matrix.ros_distro }}-ament-flake8 \
            ros-${{ matrix.ros_distro }}-ament-pep257 \
            ros-${{ matrix.ros_distro }}-ament-xmllint \
            cppcheck

      - name: ament_cpplint (nav2 C++ sources)
        run: |
          source /opt/ros/${{ matrix.ros_distro }}/setup.bash
          ament_cpplint \
            movensys_navigation_nav2/src

      - name: ament_flake8 (all Python)
        run: |
          source /opt/ros/${{ matrix.ros_distro }}/setup.bash
          ament_flake8 --config ament_flake8.ini \
            movensys_navigation_description/launch \
            movensys_navigation_nav2/launch \
            movensys_navigation_nav2/movensys_navigation_nav2

      - name: ament_xmllint (package.xml)
        run: |
          source /opt/ros/${{ matrix.ros_distro }}/setup.bash
          ament_xmllint \
            movensys_navigation_description/package.xml \
            movensys_navigation_nav2/package.xml

      - name: cppcheck (warnings only, never fails)
        run: |
          cppcheck --enable=warning,style --inline-suppr \
            --suppress=missingIncludeSystem --error-exitcode=0 \
            movensys_navigation_nav2/src
